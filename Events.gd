extends Node


signal stat_changed(statID, value)

signal time_of_day_change()

signal critera_accomplished(criteria_ID)

signal achievement_completed(achievement)
	
signal day_ended()

signal game_paused(isPaused)

signal reset_day()

signal play_sound(id)
